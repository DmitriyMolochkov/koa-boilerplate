describe('simple test for check of build', () => {
  it('should add the two numbers and return the correct result', () => {
    const numberOne = 1;
    const numberTwo = 5;
    expect(numberOne + numberTwo).toEqual(6);
  });
});
