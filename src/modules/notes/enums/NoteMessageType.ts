export enum NoteMessageType {
  noteCreated = 'note-created',
  noteUpdated = 'note-updated',
  noteRemoved = 'note-removed',
  noteStatusChanged = 'note-status-changed',
  noteExpired = 'note-expired',
}
