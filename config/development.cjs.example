/** @type {Omit<import("../src/config").ApplicationConfig, 'nodeEnv'>} */
const config = {
  serverConfig: {
    port: 8080,
    domain: 'example.com',
  },
  dbConfig: {
    connectionString: '123ruirr',
  },
};

module.exports = {
  config,
};
