/** @type {Omit<import("../src/config").ApplicationConfig, 'nodeEnv' |'isProduction'>} */
const config = {
  serverConfig: {
    port: 8085,
    domain: 'example.com',
  },
  dbConfig: {
    type: 'postgres',
    host: 'localhost',
    port: 5432,
    username: 'postgres',
    password: 'postgres',
    database: 'koa-boilerplate',
  },
};

module.exports = {
  config,
};
